enum Scope { home, flat }

enum MeterIndicatorType { currentReading, previousReading }

enum Status { loading, completed, empty, error }

enum DataStatus { loading, completed, error }

enum UnitType { previous, present }

enum Bills { home, flat }

enum ButtonState { normal, loading, updated }
